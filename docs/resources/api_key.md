---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "uptimekuma_api_key Resource - uptimekuma"
subcategory: ""
description: |-
  An API key for the Prometheus /metrics endpoint. Creating the first key also switches the server's apiKeysEnabled setting on.
  ~> The key is only returned once. Uptime Kuma stores just a hash, so key is available in state from the moment of creation and can never be recovered afterwards — an imported key has a null key.
---

# uptimekuma_api_key (Resource)

An API key for the Prometheus `/metrics` endpoint. Creating the first key also switches the server's `apiKeysEnabled` setting on.

~> **The key is only returned once.** Uptime Kuma stores just a hash, so `key` is available in state from the moment of creation and can never be recovered afterwards — an imported key has a null `key`.

## Example Usage

```terraform
# API keys authenticate the Prometheus /metrics endpoint. They are not
# credentials for the API this provider uses.
resource "uptimekuma_api_key" "prometheus" {
  name    = "Prometheus scraper"
  expires = "2027-01-01 00:00:00"
}

output "prometheus_api_key" {
  value     = uptimekuma_api_key.prometheus.key
  sensitive = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Friendly name for the key.

### Optional

- `active` (Boolean) Whether the key is enabled. Default: true.
- `expires` (String) Expiry timestamp, as `2026-12-31 23:59:00`. Leave unset for a key that never expires.

### Read-Only

- `id` (String) Numeric ID of the API key, assigned by Uptime Kuma.
- `key` (String, Sensitive) The generated key, in the form `uk<id>_<secret>`. Only ever returned at creation time.
- `status` (String) Server-computed status: `active`, `inactive` or `expired`.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# The clear-text key is only ever returned when the key is created, so an
# imported api_key has a null `key` attribute and cannot be recovered.
terraform import uptimekuma_api_key.example 1
```
