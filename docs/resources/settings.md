---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "uptimekuma_settings Resource - uptimekuma"
subcategory: ""
description: |-
  General settings of the Uptime Kuma instance.
  ~> This is a singleton resource. Uptime Kuma has no notion of creating or deleting its settings. Creating this resource adopts the current values, and destroying it only removes them from Terraform state — the server keeps whatever was last written.
  ~> Only the keys listed in settings are managed. Any key the provider does not know about is left untouched, and all exposes the full set the server reports.
---

# uptimekuma_settings (Resource)

General settings of the Uptime Kuma instance.

~> **This is a singleton resource.** Uptime Kuma has no notion of creating or deleting its settings. Creating this resource adopts the current values, and destroying it only removes them from Terraform state — the server keeps whatever was last written.

~> Only the keys listed in `settings` are managed. Any key the provider does not know about is left untouched, and `all` exposes the full set the server reports.

## Example Usage

```terraform
# Singleton resource: it adopts the instance's current settings and manages only
# the keys listed here. Destroying it does not revert anything.
resource "uptimekuma_settings" "main" {
  settings = jsonencode({
    keepDataPeriodDays = 180
    checkUpdate        = false
    checkBeta          = false
  })
}

# Discover which keys this Uptime Kuma version supports.
data "uptimekuma_settings" "current" {}

output "available_settings" {
  value = keys(jsondecode(data.uptimekuma_settings.current.settings))
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `settings` (String) Settings to manage, as a JSON object, for example `jsonencode({ keepDataPeriodDays = 180, checkUpdate = false })`. Keys absent from this object are left as they are on the server.

### Read-Only

- `all` (String) Every setting the server reports, as a JSON object. Useful for discovering which keys a given Uptime Kuma version supports.
- `id` (String) Always `settings`, since the resource is a singleton.
